# Ambient Pad Player

A simple React + Vite web app that plays **looping ambient pads** in each musical key.  
Click a button to start a pad, click another key to crossfade, or click the same key again to stop. Includes a master volume slider.

---

## Features
- 12 musical keys (A → G)
- Smooth crossfades between pads
- Click the same key again to stop
- Master volume control
- Light theme, 3 buttons per row

---

## 🚀 Setup

```bash
# Create or clone project
npm create vite@latest ambient-pads -- --template react
cd ambient-pads
npm install

# Run locally
npm run dev